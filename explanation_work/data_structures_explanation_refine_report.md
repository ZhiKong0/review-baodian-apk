# 数据结构题库解析优化报告

- 题量：262
- 题型：判断题 120; 单选题 100; 函数题 4; 编程题 15; 填空题 16; 程序填空题 7
- 章节：4. Python 基础与面向对象 62; 3. 算法复杂度与查找排序 70; 1. 线性数据结构 40; 2. 树与二叉搜索树 69; 5. Python 程序设计题 19; 5. 程序填空与代码理解 2

## 去模板化指标

- quickExplanation 唯一数：193 -> 193
- knowledgeDetail 唯一数：126 -> 125
- 低于 80 字 quick：104 -> 104
- 低于 100 字 detail：77 -> 77
- 重复题干数：99（来自 DS6A/DS6B 随机卷重复题，解析允许随同题重复）

## 优化原则

- 判断题直接指出题干中成立或错误的概念关系。
- 单选题给出正确项，并列出选项辨析。
- 函数题/编程题逐题说明输入、核心逻辑、边界和输出格式。
- 删除“题眼”“做题抓手”等元提醒式语言。

## 仍重复最多的 quick（通常是重复题干）

- 7 次：答案：T（正确）。 Python 创建对象时会自动调用 `__init__`，它负责给新对象初始化属性。 所以本题正确。
- 5 次：答案：T（正确）。 实例方法第一个形参通常写 `self`，调用方法时不需要手动传这个参数，解释器会自动绑定当前对象。 所以本题正确。
- 4 次：答案：F（错误）。 BST 的有序输出来自中序遍历，不是前序或后序遍历。 所以本题错误。 补充概念：二叉搜索树的核心不是树长什么样，而是每个结点都满足左子树小于根、右子树大于或等于根。中序遍历会把这种大小关系摊平成升序序列。
- 4 次：答案：T（正确）。 继承的意义是让子类复用父类已有属性和方法，同时子类还可以继续增加自己的属性和方法。 所以本题正确。
- 4 次：答案：T（正确）。 二叉搜索树满足左小右大，中序遍历按“左子树-根-右子树”访问，结果会从小到大排列。 所以本题正确。
- 4 次：答案：F（错误）。 中间频繁插入、删除时，顺序表要移动大量后续元素；链表定位后主要改指针。 所以本题错误。
- 3 次：答案：F（错误）。 大 O 描述的是规模 n 趋向很大时的增长趋势，不保证任意实际输入下谁一定更快。 所以本题错误。
- 3 次：答案：T（正确）。 这句话成立，因为它没有改变“Python 类、对象与方法”中的对象、前提和限制条件。 所以本题正确。

## 仍重复最多的 detail（通常是重复题干）

- 10 次：二叉树遍历题不能按字母大小猜答案，要按访问根的时机走：先序根在前，中序根在中间，后序根在后。每遇到一棵子树，都递归使用同一条规则。 补充概念：二叉搜索树的核心不是树长什么样，而是每个结点都满足左子树小于根、右子树大于或等于根。中序遍历会把这
- 8 次：判断概念题时，要把句子拆成对象、条件、结论三部分：对象是不是同一个，条件有没有少写或扩大，结论有没有说绝对。本题答案为 T（正确），说明题干给出的关系与该知识点的定义相容。复习时不要只背一句话，要看定义中的限制条件是否被完整保留。
- 7 次：`__init__` 不是普通随便命名的方法；双下划线名字固定。调用类名创建对象时，解释器先创建实例，再把实例作为 self 传给 `__init__` 完成初始化。
- 6 次：顺序存储适合按下标访问和尾部追加；链式存储适合插入、删除多的场景。若题干说这种场景更适合顺序存储，就是把两种存储结构的优势说反了。 补充概念：线性结构的难点在于区分“逻辑关系”和“存储方式”。顺序表靠连续地址支持快速下标访问；链表靠指针维持
- 6 次：继承不是简单复制代码，也不是限制子类扩展。子类先拥有父类能力，再根据需要覆盖或新增成员，所以说子类不能添加自己的内容是错的。 补充概念：实例方法定义时要给当前对象留出第一个形参，通常写 self；调用 `对象.方法()` 时，这个实参由解释
- 6 次：BST 的形状不是由集合本身唯一决定，而是由插入顺序决定。做这类题时，先确定根，再分别比较左子树、右子树内部的插入先后，找出和其他选项分叉位置不同的一项。
- 5 次：Python 列表的 `append(x)` 会把 x 作为一个整体追加到列表末尾，并且原地修改列表。若 x 是普通元素，就多一个普通元素；若 x 本身是列表，就把这个列表作为嵌套元素追加进去。`append` 方法的返回值是 None，但
- 4 次：前序是根-左-右，后序是左-右-根，它们保留的是树的结构访问顺序；只有中序把所有小于根的元素放在根前、大于根的元素放在根后，因此才形成递增序列。
