import json
from pathlib import Path


QUESTION_PATH = Path("app/src/main/assets/questions.json")


REFINEMENTS = {
    "3-13": {
        "quickExplanation": """本题考“电路交换的一次通信流程”，答案是 A、B、D。电路交换像打传统电话：先占好一条端到端电路，再传数据，最后释放资源；差错校验可以由具体协议做，但不是电路交换流程的阶段名。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 建立连接 | 选 | 电路交换必须先建立端到端连接，否则后面的独占电路不存在。 |
| B. 数据传输 | 选 | 连接建立后才进入真正传送信息的阶段。 |
| C. 差错校验 | 不选 | 这是通信可靠性/差错控制手段，不是电路交换三阶段之一。 |
| D. 释放连接 | 选 | 通信结束要拆除电路，把占用的链路和交换资源还给网络。 |

易错点：看到“通信过程”不要把所有通信可能涉及的技术都选上，本题只问电路交换固定三阶段。""",
        "knowledgeDetail": """核心知识点：
电路交换的标准过程是“建立连接、数据传输、释放连接”。它的特点是通信期间独占一条逻辑或物理通路，适合连续、大量、实时性强的数据。

题目变形：
如果题干问“电路交换为什么不适合突发数据”，答案通常围绕建立连接有开销、空闲时仍占资源、链路利用率低。

知识拓展：
电路交换、报文交换、分组交换的区分很常考。电路交换先建路再传输；报文交换以完整报文存储转发；分组交换把报文切成分组，节点按分组存储转发，互联网主要采用分组交换。""",
    },
    "3-5": {
        "quickExplanation": """本题按“作用范围/覆盖范围”给计算机网络分类，A、B、C、D 全选。它不是按拓扑、交换方式或传输介质分类。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 广域网 | 选 | WAN 覆盖跨城市、跨地区、跨国家的大范围网络。 |
| B. 城域网 | 选 | MAN 通常覆盖一个城市或城区范围。 |
| C. 局域网 | 选 | LAN 覆盖办公室、楼宇、校园等较小区域。 |
| D. 个人区域网 | 选 | PAN 覆盖个人设备周边，如蓝牙耳机、手表、手机互联。 |

记忆：范围从大到小可粗略记为 WAN > MAN > LAN > PAN。""",
        "knowledgeDetail": """核心知识点：
按作用范围分类，计算机网络常见类别包括广域网、城域网、局域网和个人区域网。这个分类看的是网络覆盖空间和组织规模。

题目变形：
若题目给出“校园网、办公室网”，通常归局域网；给出“一个城市的政务网络”，常归城域网；给出“跨省公司专线互联”，常归广域网。

知识拓展：
同一个网络也可以从不同角度分类。例如校园网按范围是局域网，按拓扑可能是星型/树型，按传输介质可能是有线加无线混合。审题时先看题干指定的分类口径。""",
    },
    "3-7": {
        "quickExplanation": """本题问“教学采用的五层协议体系结构”，答案是 A、B、C。五层模型是：物理层、数据链路层、网络层、运输层、应用层；会话层属于 OSI 七层模型，在五层教学模型中不单列。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 应用层 | 选 | 五层模型最高层，包含 HTTP、DNS、FTP 等应用协议。 |
| B. 运输层 | 选 | 负责端到端进程通信，典型协议 TCP/UDP。 |
| C. 网络层 | 选 | 负责分组转发和路由选择，核心协议 IP。 |
| D. 会话层 | 不选 | 会话层是 OSI 七层里的第 5 层，五层模型把它并入应用层相关功能。 |

小白抓法：看到“五层”就不要把 OSI 七层的会话层、表示层单独选进去。""",
        "knowledgeDetail": """核心知识点：
五层协议体系结构从低到高是物理层、数据链路层、网络层、运输层、应用层。它吸收了 OSI 分层思想，又贴近 TCP/IP 的实际使用。

题目变形：
若题干改成“OSI 七层参考模型包含哪些层”，会话层、表示层、应用层都应属于 OSI；若题干是“TCP/IP 四层”，则常写网络接口层、网际层、运输层、应用层。

知识拓展：
分层模型的考法常把不同模型的术语混放。网络层是 OSI/五层术语；网际层是 TCP/IP 四层术语；会话层和表示层只在 OSI 七层中单列。""",
    },
    "3-58": {
        "quickExplanation": """本题问 ICMP 报文的“两大类别”，答案是 A、B。ICMP 是 IP 的辅助协议，主要用来报告差错和做询问诊断；路由更新、链路状态属于路由协议的工作内容。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 差错报告 | 选 | 目的不可达、超时等都属于 ICMP 差错报告类。 |
| B. 询问 | 选 | 回送请求/回答用于 ping，这属于 ICMP 询问类。 |
| C. 路由更新 | 不选 | 路由更新由 RIP、OSPF、BGP 等路由协议处理，不是 ICMP 分类。 |
| D. 链路状态 | 不选 | 链路状态是 OSPF 一类协议的路由信息，不是 ICMP 报文大类。 |

易混：ping 用 ICMP，但路由表怎么更新不是 ICMP 的职责。""",
        "knowledgeDetail": """核心知识点：
ICMP 报文分为差错报告报文和询问报文。它封装在 IP 数据报中，用于让主机或路由器报告网络层问题、辅助网络诊断。

题目变形：
若问 ping 使用什么协议，答 ICMP；若问 traceroute/tracert 常利用什么机制，通常涉及 ICMP 超时报文或端口不可达报文。

知识拓展：
ICMP 不是运输层协议，也不负责传输用户数据。它服务于 IP 层，帮助发现“不可达、超时、参数问题”等网络层异常。""",
    },
    "3-69": {
        "quickExplanation": """本题考网络拓扑结构的基本类型，A、B、C、D 全选。拓扑结构描述的是结点和链路在逻辑或物理上的连接形态。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 总线型 | 选 | 多个结点共享一条主干线路，早期共享式以太网常用这个思路。 |
| B. 星型 | 选 | 结点围绕中心设备连接，现代交换式局域网常见。 |
| C. 环型 | 选 | 结点首尾相接形成闭环，令牌环就是典型例子。 |
| D. 树型 | 选 | 星型的层次扩展，适合分层汇聚的网络结构。 |

别漏选：这题不是问“最常用是哪种”，而是问“可分为哪些”。""",
        "knowledgeDetail": """核心知识点：
网络拓扑常见类型包括总线型、星型、环型、树型、网状型等。它们反映结点之间的连接关系，而不是协议层次。

题目变形：
若题目问 10BASE-T 的拓扑，常答星型；若问早期共享总线以太网，则是总线型。

知识拓展：
物理拓扑和逻辑拓扑可能不同。例如使用交换机连接看起来是星型物理拓扑，但上层通信逻辑还要看交换、广播域、VLAN 等机制。""",
    },
    "3-16": {
        "quickExplanation": """本题问报文交换特点，答案是 A、B、D。报文交换以“完整报文”为单位在中间结点存储转发，因此不用先建立专用电路，但长报文会导致排队和转发时延大，不适合实时语音。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 存储转发 | 选 | 中间结点先收完整报文，再选择下一跳转发。 |
| B. 长报文整体传输 | 选 | 报文不切小分组，单位可能很长。 |
| C. 适合实时语音 | 不选 | 实时语音怕长时延和时延抖动，报文交换不合适。 |
| D. 时延大 | 选 | 完整接收、排队、查找转发路径都会增加时延。 |

抓题眼：“报文”两个字强调整体处理；“实时”通常和报文交换冲突。""",
        "knowledgeDetail": """核心知识点：
报文交换采用存储转发，交换单位是完整报文。它不需要通信前建立专用电路，但中间结点需要足够缓存，且转发时延较大。

题目变形：
如果题目问互联网为什么更常用分组交换，常答分组长度受限、可并行排队转发、链路利用率高、对突发数据更友好。

知识拓展：
报文交换和分组交换都属于存储转发思想，但分组交换把长报文拆成小分组，使中间结点缓存压力和等待时间更可控。""",
    },
    "3-28": {
        "quickExplanation": """本题考香农公式 C = W log2(1 + S/N)，答案是 A、B、C。香农公式描述有噪声信道的极限信息传输率，直接用到带宽、信号功率、噪声功率；码元进制 M 是奈氏准则/调制编码里常见的量，不直接出现在香农公式中。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 带宽 W | 选 | W 是公式里的信道带宽，带宽越大容量上限越高。 |
| B. 信号功率 S | 选 | S 与 N 组成信噪比 S/N。 |
| C. 噪声功率 N | 选 | 噪声越大，S/N 越低，容量越低。 |
| D. 码元进制 M | 不选 | M 影响每个码元携带 bit 数，但不是香农公式的直接参数。 |

易错：奈氏准则常见 M，香农公式常见 S/N。""",
        "knowledgeDetail": """核心知识点：
香农公式给出有噪声信道容量上限：C = W log2(1 + S/N)。W 是带宽，S/N 是信噪比。

题目变形：
若题目给信噪比 dB，需要先换算成普通比值再代入；若题目说无噪声理想低通信道最高码元率，则更可能考奈氏准则。

知识拓展：
奈氏准则强调带宽和码元速率上限，香农公式强调噪声条件下的信息传输率上限。二者都是物理层计算题高频来源。""",
    },
    "3-11": {
        "quickExplanation": """本题问常用网络性能指标，A、B、C、D 全选。这四个分别从“标称能力、最高能力、实际通过量、时间开销”描述网络性能。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 速率 | 选 | 表示单位时间传输的数据量，常用 bit/s。 |
| B. 带宽 | 选 | 在计算机网络中常表示链路可达到的最高数据率。 |
| C. 吞吐量 | 选 | 表示实际单位时间通过网络的数据量，受拥塞和开销影响。 |
| D. 时延 | 选 | 表示数据从源到目的经历的时间，是体验和性能关键指标。 |

辨析：带宽像“路的上限”，吞吐量像“实际跑过的车流”。""",
        "knowledgeDetail": """核心知识点：
计算机网络常用性能指标包括速率、带宽、吞吐量、时延、时延带宽积、往返时间 RTT、利用率等。

题目变形：
若问总时延组成，通常是发送时延、传播时延、处理时延、排队时延；若问实际传输能力，常看吞吐量而不是只看带宽。

知识拓展：
利用率不是越高越好。利用率接近 1 时，排队时延会急剧增大，所以网络设计通常要留冗余。""",
    },
    "3-42": {
        "quickExplanation": """本题问局域网典型特征，A、B、C、D 全选。局域网通常由一个单位在有限范围内建设和管理，因此距离短、介质可控、速率高、误码率低。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 范围小 | 选 | 局域网覆盖办公室、楼宇、校园等有限区域。 |
| B. 高速率 | 选 | 局域网链路短、设备专用，常能提供较高数据率。 |
| C. 低误码 | 选 | 传输距离短、介质质量可控，误码率通常较低。 |
| D. 单一组织 | 选 | 局域网一般由一个组织拥有、建设和管理。 |

别只记“范围小”：管理归属和误码特征也常被拿来考。""",
        "knowledgeDetail": """核心知识点：
局域网 LAN 的典型特征是覆盖范围较小、传输速率较高、误码率较低、通常由单一组织管理。

题目变形：
若题干给“跨城市、跨国家”，应偏广域网；给“城市范围”，偏城域网；给“办公室/校园”，偏局域网。

知识拓展：
以太网是最常见的局域网技术之一，但“局域网”是范围和组织概念，“以太网”是具体的数据链路层/物理层技术体系。""",
    },
    "3-62": {
        "quickExplanation": """本题问 IPv6 定义的地址类型，答案是 A、B、C。IPv6 取消了广播地址，很多原来广播完成的事情改用多播完成；任播是 IPv6 中很重要的地址类型。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 单播 | 选 | 一对一通信，标识一个接口。 |
| B. 多播 | 选 | 一对多，把分组发给一组接口。 |
| C. 任播 | 选 | 发给一组接口中“路由上最近”的一个。 |
| D. 广播 | 不选 | IPv6 不定义广播地址，相关功能多由多播替代。 |

易错：IPv4 有广播，IPv6 没有广播；不要把两代 IP 的地址类型混用。""",
        "knowledgeDetail": """核心知识点：
IPv6 地址类型包括单播、多播和任播，不包括广播。

题目变形：
若题目问 IPv4 常见通信方式，可出现单播、广播、多播；若问 IPv6 地址类型，看到广播要排除。

知识拓展：
任播常用于让用户访问同一服务的最近节点，例如 DNS 根服务器部署可使用任播思想提高访问效率和可靠性。""",
    },
    "3-44": {
        "quickExplanation": """本题考传统以太网 CSMA/CD 的争用期，答案是 A、B、C。争用期就是碰撞窗口，表示最远两端信号往返传播的时间；传统 10Mb/s 以太网常记 51.2 μs。D 错在说争用期内无碰撞，实际上争用期就是用来发现碰撞的关键窗口。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 碰撞窗口 | 选 | 争用期也叫碰撞窗口，发送方需在这段时间内能发现最坏情况碰撞。 |
| B. 51.2μs | 选 | 传统 10Mb/s 以太网规定的争用期常考值。 |
| C. 往返传播时延 | 选 | 本质是最远两个站点之间的来回传播时间。 |
| D. 争用期内无碰撞 | 不选 | 恰恰相反，争用期内可能暴露碰撞。 |

记忆：争用期不是安全期，而是“能不能及时发现碰撞”的窗口。""",
        "knowledgeDetail": """核心知识点：
CSMA/CD 中争用期为最远端到端传播时延的两倍，也叫碰撞窗口。最小帧长要保证发送站在发送完前仍能检测到最远碰撞。

题目变形：
若给出距离、传播速率和数据率，让求最小帧长，通常先算往返传播时间，再乘以数据率。

知识拓展：
交换式全双工以太网不存在共享信道碰撞，因此不再使用 CSMA/CD；争用期主要是传统共享式半双工以太网考点。""",
    },
    "3-19": {
        "quickExplanation": """本题按“资源子网/通信子网”区分设备，答案是 A、B、D。资源子网面向用户提供计算、存储、打印等资源；通信子网负责把数据送过去，路由器属于通信子网核心设备。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 主机 | 选 | 主机运行应用，提供或使用计算资源。 |
| B. 网络打印机 | 选 | 打印机是共享硬件资源，属于资源子网范畴。 |
| C. 路由器 | 不选 | 路由器负责分组转发和路径选择，属于通信子网。 |
| D. 服务器 | 选 | 服务器提供文件、Web、数据库等应用资源。 |

抓法：提供资源的是资源子网，负责传输/转发的是通信子网。""",
        "knowledgeDetail": """核心知识点：
从功能组成看，计算机网络可分为资源子网和通信子网。资源子网提供用户资源，通信子网提供数据传输能力。

题目变形：
若选项出现交换机、路由器、通信线路，通常偏通信子网；出现主机、服务器、打印机、数据库，通常偏资源子网。

知识拓展：
这个分类是早期计算机网络组成视角，和“互联网边缘/核心”有相似直觉：边缘主机提供应用，核心网络负责转发。""",
    },
    "3-47": {
        "quickExplanation": """本题考 Hub 的工作特点，答案是 A、B、D。Hub 是物理层设备，只会把信号再生并广播到其他端口；所有端口共享带宽，不能隔离冲突域。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 物理层 | 选 | Hub 不识别 MAC 帧地址，只处理电信号/比特。 |
| B. 共享带宽 | 选 | 所有端口竞争同一共享介质，总带宽被共享。 |
| C. 隔离冲突域 | 不选 | Hub 会把站点放进同一个冲突域，不能隔离。 |
| D. 广播转发 | 选 | 收到信号后向除入端口外的其他端口转发。 |

和交换机对比：交换机能隔离冲突域，Hub 不能。""",
        "knowledgeDetail": """核心知识点：
集线器 Hub 工作在物理层，所有端口共享带宽，采用广播式转发，不能隔离冲突域，也不能隔离广播域。

题目变形：
若问“哪个设备不能隔离碰撞域”，常选集线器；若问“每个端口一个冲突域”，通常是交换机。

知识拓展：
Hub、交换机、路由器的层次要分清：Hub 物理层，交换机数据链路层，路由器网络层。层次越高，越能根据更高层地址做转发和隔离。""",
    },
    "3-24": {
        "quickExplanation": """本题按“信息交互方向”分类通信方式，答案是 A、B、C。单工、半双工、全双工描述双方能不能发、能不能同时发；并行描述的是一次传多位，不是交互方向。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 单工 | 选 | 信息只能单方向传输，如单向广播。 |
| B. 半双工 | 选 | 双方都能发，但同一时刻只能一个方向发。 |
| C. 全双工 | 选 | 双方可以同时发送和接收。 |
| D. 并行 | 不选 | 并行/串行是数据位传输方式，不是信息交互方向分类。 |

易错：半双工不是“半条线路”，而是不能同时双向传。""",
        "knowledgeDetail": """核心知识点：
按信息交互方向，通信方式分为单工、半双工、全双工。

题目变形：
若题干说“两个方向可以传输但不能同时”，答半双工；说“两个方向同时传输”，答全双工；说“只能一个方向”，答单工。

知识拓展：
串行/并行关注数据位如何排列传输；同步/异步关注时钟和字符边界；单工/半双工/全双工关注通信方向，三组分类口径不同。""",
    },
    "3-3": {
        "quickExplanation": """本题问分组交换的核心优点，A、B、C、D 全选。教材常把分组交换概括为高效、灵活、迅速、可靠：共享链路提高利用率，分组可独立转发，不必建立专用电路，并能通过路由选择和差错处理增强可靠性。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 高效 | 选 | 采用统计复用，多用户按需共享链路，空闲资源不被长期独占。 |
| B. 灵活 | 选 | 分组可按网络状态选择不同路径，适应拓扑和流量变化。 |
| C. 迅速 | 选 | 不需要像电路交换那样先建立端到端专用电路。 |
| D. 可靠 | 选 | 分组可重传、绕路或由上层保障端到端可靠。 |

注意：这里“可靠”是相对电路/报文交换的综合优点，不等于 IP 本身提供可靠传输。""",
        "knowledgeDetail": """核心知识点：
分组交换把长报文拆分成较短分组，各分组带有控制信息，在网络中存储转发。优点常概括为高效、灵活、迅速、可靠。

题目变形：
若题目问分组交换缺点，常见答案是分组需携带首部开销、可能排队、可能乱序、产生时延抖动。

知识拓展：
互联网的 IP 层采用尽最大努力的分组交付；可靠性通常由 TCP 等运输层协议补足。不要把“分组交换体系可提高可靠性”和“IP 一定可靠”混为一谈。""",
    },
    "3-2": {
        "quickExplanation": """本题问“从工作方式角度”看互联网由什么组成，答案是 A、B。教材把互联网分为边缘部分和核心部分：边缘部分是主机和应用所在处，核心部分是大量网络和路由器组成的转发系统。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 边缘部分 | 选 | 端系统、客户端、服务器等运行网络应用，属于边缘部分。 |
| B. 核心部分 | 选 | 路由器和网络互连组成核心，为边缘主机提供连通。 |
| C. 接入部分 | 不选 | 接入网很重要，但不是本题“工作方式”二分法的标准项。 |
| D. 传输部分 | 不选 | 不是教材对互联网组成的标准名称，容易和运输层混淆。 |

审题眼：“工作方式角度”对应边缘/核心。""",
        "knowledgeDetail": """核心知识点：
互联网从工作方式看由边缘部分和核心部分组成。边缘部分的端系统运行应用程序，核心部分通过分组交换和路由转发提供连通。

题目变形：
若问边缘端系统之间的通信方式，常答 C/S 和 P2P；若问核心部分主要技术，常答分组交换。

知识拓展：
接入网连接端系统到互联网边缘或 ISP，但“边缘/核心”是教材解释互联网工作方式的主框架，不能把接入网混成第三个并列组成部分。""",
    },
    "3-63": {
        "quickExplanation": """本题问内部网关协议 IGP，答案是 A、B。IGP 用在一个自治系统内部，RIP 和 OSPF 都是典型 IGP；BGP 用在自治系统之间，PPP 是链路层协议。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. RIP | 选 | RIP 是自治系统内部使用的距离向量路由协议。 |
| B. OSPF | 选 | OSPF 是自治系统内部使用的链路状态路由协议。 |
| C. BGP | 不选 | BGP 是外部网关协议 EGP，用于 AS 之间。 |
| D. PPP | 不选 | PPP 是点到点数据链路层协议，不是路由协议。 |

记忆：RIP/OSPF 在 AS 内，BGP 在 AS 间。""",
        "knowledgeDetail": """核心知识点：
路由协议按作用范围可分为内部网关协议 IGP 和外部网关协议 EGP。RIP、OSPF 属于 IGP，BGP 属于 EGP。

题目变形：
若问“距离向量协议”，常选 RIP；若问“链路状态协议”，常选 OSPF；若问“自治系统之间”，常选 BGP。

知识拓展：
自治系统 AS 是由同一管理机构控制、使用统一路由策略的一组网络。IGP 解决 AS 内部怎么走，BGP 解决 AS 之间怎么交换可达性。""",
    },
    "3-38": {
        "quickExplanation": """本题问 CRC 特点，答案是 A、B、D。CRC 用模 2 除法/异或做差错检测，能发现许多错误，但它本身只检错、不纠错；接收方发现错帧通常丢弃。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 差错检测 | 选 | CRC 的主要目的就是检测帧中是否可能有差错。 |
| B. 模2运算 | 选 | CRC 计算不进位不借位，本质是按位异或。 |
| C. 可纠错 | 不选 | CRC 通常不能定位并自动改正错误比特。 |
| D. 只检错 | 选 | 教材高频结论：CRC 只负责检错，不等于可靠传输。 |

易错：检错不等于纠错，更不等于自动重传。""",
        "knowledgeDetail": """核心知识点：
循环冗余检验 CRC 是数据链路层常用差错检测技术，采用模 2 运算生成冗余码，接收方用同样规则检查余数。

题目变形：
若题干说“CRC 能纠正差错”通常错误；若说“仅使用 CRC 只能做到对帧的无差错接受”，通常正确。

知识拓展：
可靠传输需要检错、确认、重传、序号等机制配合。CRC 只是发现问题的眼睛，不是修复问题的手。""",
    },
    "3-10": {
        "quickExplanation": """本题问计算机网络核心功能，A、B、C、D 全选。网络不是只“能上网”，它的基本价值包括通信、共享、协同处理和提高可靠性。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 数据通信 | 选 | 主机之间传送数据是计算机网络最基本功能。 |
| B. 资源共享 | 选 | 可共享文件、打印机、数据库、计算资源等。 |
| C. 分布式处理 | 选 | 多台计算机可协作完成任务或分担负载。 |
| D. 提升可靠性 | 选 | 冗余链路、备份服务器等能提高系统可用性。 |

这题属于概念总览题，四项都对应网络的正向功能。""",
        "knowledgeDetail": """核心知识点：
计算机网络的核心功能包括数据通信、资源共享、分布式处理和提高可靠性等。

题目变形：
若题目问“最基本功能”，通常优先答数据通信；若问“主要功能/核心功能”，资源共享也常是重要答案。

知识拓展：
后来很多网络应用，如云计算、分布式存储、协同办公，本质上都是这些基础功能的扩展。""",
    },
    "3-17": {
        "quickExplanation": """本题问 TCP/IP 四层体系结构，A、B、C、D 全选。四层从低到高是网络接口层、网际层、运输层、应用层。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 网络接口层 | 选 | 对应底层接入网络，涵盖链路和物理相关功能。 |
| B. 网际层 | 选 | 核心是 IP，负责跨网络分组转发。 |
| C. 运输层 | 选 | TCP/UDP 提供端到端进程通信。 |
| D. 应用层 | 选 | HTTP、DNS、FTP 等协议位于应用层。 |

注意术语：TCP/IP 叫“网际层”，五层/OSI 常叫“网络层”。""",
        "knowledgeDetail": """核心知识点：
TCP/IP 四层模型包括网络接口层、网际层、运输层、应用层。

题目变形：
若题目改问 OSI 七层，要补上物理层、数据链路层、网络层、运输层、会话层、表示层、应用层。

知识拓展：
TCP/IP 是事实上的工业标准，OSI 是重要参考模型。考试常用五层模型教学：物理层、数据链路层、网络层、运输层、应用层。""",
    },
    "3-18": {
        "quickExplanation": """本题问分层体系结构的优势，A、B、C、D 全选。分层的核心是“各层相对独立、通过接口协作”，这样便于维护、替换和标准化。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 独立 | 选 | 每层只关注本层职责和上下层接口。 |
| B. 易维护 | 选 | 某层实现变化时，只要接口不变，对其他层影响较小。 |
| C. 上层调用下层 | 选 | 上层通过服务接口使用下层提供的功能。 |
| D. 促进标准化 | 选 | 清晰接口和协议边界便于不同厂商实现互通。 |

抓法：分层不是为了变复杂，而是为了把复杂网络拆成可管理的模块。""",
        "knowledgeDetail": """核心知识点：
网络分层的优点包括降低复杂性、便于标准化、各层独立演进、便于学习和维护。

题目变形：
若题目问“协议”和“服务”的区别，协议是同层实体之间通信规则，服务是下层向上层提供的能力。

知识拓展：
分层也有代价，例如每层首部带来开销，严格分层可能降低某些优化空间，但考试基础题通常考它的优点。""",
    },
    "3-4": {
        "quickExplanation": """本题问数据传输总时延组成，A、B、C、D 全选。网络时延不是单一时间，而是发送、传播、处理、排队四类时间叠加。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 发送时延 | 选 | 把分组所有比特推上链路需要时间，约为数据长度/发送速率。 |
| B. 传播时延 | 选 | 信号在介质中从一端跑到另一端需要时间，约为距离/传播速率。 |
| C. 处理时延 | 选 | 结点检查首部、差错检测、查表转发都要时间。 |
| D. 排队时延 | 选 | 分组等待输出链路空闲会产生排队时间。 |

易错：发送时延是“发完要多久”，传播时延是“路上跑多久”。""",
        "knowledgeDetail": """核心知识点：
总时延通常由发送时延、传播时延、处理时延和排队时延组成。

题目变形：
计算发送时延用分组长度除以发送速率；计算传播时延用链路长度除以传播速率。

知识拓展：
排队时延最不稳定，网络拥塞时会急剧增大；这也是利用率不能无限接近 100% 的原因。""",
    },
    "3-9": {
        "quickExplanation": """本题问电路交换特点，答案是 A、B、D。电路交换要经历三阶段，通信期间独占资源，适合连续大量数据；突发数据常有空闲间隙，独占资源会造成利用率低，所以 C 不选。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 分三阶段 | 选 | 建立连接、数据传输、释放连接是电路交换基本流程。 |
| B. 独占资源 | 选 | 通信期间相关链路资源被这次连接占用。 |
| C. 突发数据利用率高 | 不选 | 突发流量有大量空闲间隔，独占电路反而浪费。 |
| D. 连续大量数据快 | 选 | 建好电路后可稳定连续传输，适合实时语音等场景。 |

判断方法：电路交换强在连续实时，弱在突发共享。""",
        "knowledgeDetail": """核心知识点：
电路交换先建立专用连接，通信期间独占资源，结束后释放连接。优点是传输稳定、实时性好；缺点是建立连接有开销，资源利用率可能低。

题目变形：
若题目说“无需建立连接、按分组共享链路”，更像分组交换；若说“整份报文存储转发”，更像报文交换。

知识拓展：
电话网是电路交换的典型例子；互联网主要采用分组交换，因为互联网数据常呈突发性。""",
    },
    "3-14": {
        "quickExplanation": """本题问 OSI 七层参考模型包含哪些层，答案是 A、B、D。会话层、表示层、应用层都属于 OSI；“网际层”是 TCP/IP 四层模型里的叫法，不是 OSI 标准层名。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 会话层 | 选 | OSI 七层中的第 5 层，负责会话管理相关功能。 |
| B. 表示层 | 选 | OSI 七层中的第 6 层，处理表示、编码、加密等。 |
| C. 网际层 | 不选 | 网际层是 TCP/IP 术语；OSI 对应位置叫网络层。 |
| D. 应用层 | 选 | OSI 最高层，直接面向网络应用。 |

易错：OSI 说“网络层”，TCP/IP 说“网际层”。""",
        "knowledgeDetail": """核心知识点：
OSI 七层从低到高为物理层、数据链路层、网络层、运输层、会话层、表示层、应用层。

题目变形：
若题目问五层模型，会话层和表示层通常不单列；若问 TCP/IP 四层，使用“网际层”这个术语。

知识拓展：
考试常把 OSI、TCP/IP、五层模型混在选项里。做题先确认题干指定哪一种模型，再判断层名是否属于该模型。""",
    },
    "3-21": {
        "quickExplanation": """本题问网络利用率，答案是 A、C、D。利用率可以分信道利用率和网络利用率；利用率过高会让排队时延急剧增大，所以不是越高越好；全网利用率可看作各信道利用率的加权平均。

| 选项 | 结论 | 辨析 |
|---|---|---|
| A. 分信道和网络利用率 | 选 | 教材会区分单条信道利用率和全网整体利用率。 |
| B. 越高越好 | 不选 | 利用率接近满载时排队严重，时延会快速上升。 |
| C. 过高时延剧增 | 选 | 这是网络排队论中的典型结论，也是拥塞直观表现。 |
| D. 全网加权均值 | 选 | 网络利用率可由各信道利用率按权重综合得到。 |

记忆：利用率太低浪费，太高拥塞；不是“越满越好”。""",
        "knowledgeDetail": """核心知识点：
网络利用率反映网络资源被使用的程度。利用率升高会提高资源使用效率，但过高会造成排队时延显著增加。

题目变形：
若题目问“为什么高速链路仍会卡”，可从瓶颈链路、排队时延、拥塞、协议开销等角度解释。

知识拓展：
带宽、吞吐量、利用率和时延要一起看。高带宽不等于低时延，高利用率也不等于好体验。""",
    },
}


def main() -> None:
    questions = json.loads(QUESTION_PATH.read_text(encoding="utf-8"))
    by_label = {q.get("label"): q for q in questions}
    missing = []
    changed = []
    for label, fields in REFINEMENTS.items():
        question = by_label.get(label)
        if question is None:
            missing.append(label)
            continue
        for key, value in fields.items():
            question[key] = value
        changed.append(label)
    QUESTION_PATH.write_text(json.dumps(questions, ensure_ascii=False, indent=2), encoding="utf-8")
    print(f"changed={len(changed)} {changed}")
    print(f"missing={missing}")


if __name__ == "__main__":
    main()
